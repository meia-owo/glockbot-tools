# スマスロ天井・状態把握メモ（3機種タブ）

版: **GlockBOT 0.2.1**（趣味枠／Pages可）

ホールでその場で使える **天井・状態把握メモ**。機種タブ切替で定数・示唆早見・天井モードを差し替える。  
正の仕様: `/workspace/specs/smaslo-ceiling-memo-0.1.md`（親UX）  
0.2 追加: `/workspace/specs/smaslo-ceiling-memo-0.2-session-log.md`（南国SPECIAL 初当たりセット／連チャン／スルー）  
0.2.1 追加: `/workspace/specs/smaslo-ceiling-memo-0.2.1-hint-log.md`（南国 示唆ログ・ボーナス間／通常待ち）  
旧単機 `apps/yabachiba-memo/` は本アプリへ統合（誘導ページのみ残置）。

## 開き方

1. `index.html` をブラウザで開く（`file://` 可・**CDNなし・外部通信なし**）
2. または GitHub Pages 等に静的配置（趣味枠・掲載は任意）

パス: `apps/smaslo-ceiling-memo/`

## 機種タブ

| タブ | id | 主軸 | G天井 | 第2カウンタ |
|---|---|---|---|---|
| ヤバチバ | `yabachiba` | G × チェリー | 999 | 40（設定変更時30） |
| チバリヨ2 | `chibariyo2` | G × チェリー | 通常999／リセット350／600 | 45（任意トグルで通常系40） |
| 南国SPECIAL | `nangoku-sp` | G × スイカ | 通常799／短縮500 | 0–9（3/5/7マーク・9濃厚） |

切替後も **機種別 localStorage** で値が残る。

## 操作

| 操作 | 内容 |
|---|---|
| 機種タブ | 上部固定。切替でキャップ・ラベル・示唆が差し替わる |
| 天井モード | ヘッダ付近のセレクト／設定変更トグル／通常系40トグル |
| Gスライダー | 大ドラッグ ＋ ±1/±10 ＋ 数値直入 |
| 第2 +1/−1 | 大きいボタン主（細いスライダー可） |
| **初当たりセット**（南国のみ 0.2） | ボード＋入力の下、示唆早見の上。進行中と終了を分けて記録 |
| **示唆を残す**（南国のみ 0.2.1） | 進行中カード／通常待ちから hints 選択で区間付きログ。終了セットはタップで区間別一覧 |
| 示唆早見 | 状態ボード直下の折りたたみ（初期閉じ可）。見たもの→意味・強度ラベル |
| 副次メモ | さらに奥。設定表・立ち回り。南国の今日スルーはセット由来（読取専用） |
| 当該機種リセット | アクティブ機種のカウンタのみクリア（セット履歴は別キー） |
| 全機種JSON | 書出／読込（3機種の天井状態まとめて。セット履歴は別） |

## 初当たりセット／連チャン／スルー（南国SPECIAL・0.2）

| 用語 | 意味 |
|---|---|
| **初当たりセット** | 通常からの1回の初当たりを起点にした一連。ボーナスを時系列で積む単位 |
| **進行中セット** | まだ終了していないセット。画面上で「いま」と強調 |
| **終了セット** | やめた／通常に戻したセット。連チャン数とスルー判定が確定 |
| **連チャン数** | そのセット内のボーナス回数（初当たりボーナスを **1連目** と数える） |
| **BIG / REG** | ボーナス種別。必ず区別して記録 |
| **スルー** | **セット単位**。その初当たりセットが **飛翔に繋がらずに終了**したらスルー +1。日次のマニュアル +1 は作らない |

### 操作フロー（南国タブのみ表示）

1. 進行中なし → 「初当たりを記録」: 初当G（既定＝ボードG）・任意スイカ・1発目 BIG/REG → 進行中開始（連目=1）
2. 進行中あり → `[+ BIG] [+ REG]` で連チャン追加
3. 終了: `[飛翔に繋がった]`（スルーに数えない）／`[スルーで終了]`（今日スルー+1）／`[やめた（判定保留）]`
4. 集計バー: 今日スルー数／今日セット数／最大連
5. 「セット全クリア」は確認ダイアログ付き

他2機種タブにはセットログUIを出さない。

### 示唆ログ（南国のみ・0.2.1）

1. 進行中カード（または通常待ちカード）の「示唆を残す」→ 既存 `hints[]` を group／設定見出し付きで選択
2. 現在開いている区間に1件追加（任意メモ可）。断定UIなし
3. カード直下に今区間の直近数件を小さく表示
4. 終了セット行をタップすると区間ラベルごとの示唆一覧
5. 示唆早見カードにも「この行をログへ」（進行中／待ち中）


## localStorage キー

```
smaslo-ceiling-memo:v1:<machineId>         # 機種別天井状態（0.1系・壊さない）
smaslo-ceiling-memo:v1:activeTab           # 最後に開いたタブ
smaslo-ceiling-memo:v2:nangoku-sp:sets     # 南国 初当たりセット履歴（0.2）
```

`machineId` ∈ `yabachiba` / `chibariyo2` / `nangoku-sp`

### セットデータ形（要約）

```js
{
  dayKey: "YYYY-MM-DD",
  activeId: "set_..." | null,
  openSegment: { segmentKey, segmentLabel, setId|null } | null,
  waitingHints: null | {
    segmentKey: "post-set",
    segmentLabel: "セット後〜次初当",
    endedSetId, events: hintEvent[], openedAt
  },
  sets: [{
    id, startedAt, endedAt: null|ISO,
    firstHitG, firstHitSuika?,
    bonuses: [{ n, type: "BIG"|"REG", at }],
    linkedHisho: true|false|null,
    note: "",
    hintEvents: [{
      id, segmentKey, segmentLabel,
      when, means, strength, section, group, note, at
    }]
  }]
}
```

- `renchanCount = bonuses.length`
- 今日スルー = 今日に属する終了セットで `linkedHisho === false` の件数
- 日付跨ぎ: `dayKey` を今日に更新し履歴は保持。「今日」表示は `startedAt`／`endedAt` のローカル日付でフィルタ
- **区間**: 初当開始 → `after-start`（1連目中）／ボーナス追加ごとに `between-n-(n+1)`／セット終了 → `waitingHints`（post-set）／次初当で waiting を前セットへアーカイブ
- 旧セット（hintEvents なし）は読込時に `[]` へマイグレーション（破壊しない）

## 先着ラベル（リード論理）

- `remG = max(0, gCap - g)` / `remC2 = max(0, c2Cap - c2)`
- 進捗比を比較。高い方が天井に近い
- **近い**: 残り ≤ キャップの 10% **または** ≤50G／チェリー系 ≤5／スイカ系 ≤1
- 比がほぼ同じ（\|差\| &lt; 0.03）かつ両方近い → 「ほぼ同時」
- どちらも近くない（進捗 &lt; 0.55）→ 「どちらも余裕」
- それ以外 → 「G先」／「チェリー先」／「スイカ先」

## 示唆早見

- `hints[] = { when, means, strength, section, group? }`
- strength: `濃厚` / `示唆` / `弱` / `調査中`
- 南国の PUSH「可!／良!／極み!」は **設定示唆**セクション（モード示唆と分離）
- **自動判定しない**。「設定○です」断定UIなし

## 機種別出典・注意

数値は解析サイト掲載（独自調べ含む）の寄せ集め。メーカー公式の全数値公表ではない。今後修正されうる。

### ヤバチバ（LヤバチバZM／ネット・製造オーゼキ）

- 天井 999G+α または チェリー40（設定変更後30）
- ベル集中突入率・機械割は旧単機メモと同系の参考表
- 花笠／バックランプ／トップランプ／虹7は照合用の短いカード

### チバリヨ2（Lチバリヨ2ZB／ネット・製造オーゼキ）

- 通常999／リセット350 or 600、チェリー45（通常系40説はトグル）
- 花笠ボイス・トップランプ・単チェ等は次回モード寄り示唆
- 脚注: ボーナス後32Gやめ目安
- 設定表はプレースホルダ（最新解析で要更新）
- 2プラスは別機種（本タブ対象外）

### 南国SPECIAL（L南国育ちSPM1／アムテックス・平和）

- 通常799／短縮500、スイカ9濃厚・3/5/7マーク
- バタフライ終了フラッシュ色＝モード寄り／PUSHボイス＝設定示唆
- 脚注: ときめき後やめ／赤虹続行寄り
- 設定表・狙い目はプレースホルダ（自己判断）
- **0.2**: 初当たりセットログ（スルー＝飛翔に繋がらず終了）
- **0.2.1**: 示唆ログ（ボーナス間／セット後〜次初当の通常待ち）

## 共通免責

- **非公式** の個人用メモ
- **煽り・「勝てる」断定はしない**
- 個人収支・店舗名はクラウドに送らない（localStorage のみ）
- ギャンブル依存に注意。のめり込みを感じたら休憩・相談を

## 自己チェック

```bash
node _selfcheck/run.js
```

3機種 id・hints 非空、リードラベル、天井モード切替、LSキー（v1+v2）、CDN/fetch なし、版文字列、セット／スルー集計、示唆区間開閉ヘルパを検証。

## やらないこと（0.2 / 0.2.1）

- 他機種タブへのログUI展開
- カメラ／音声の自動ボーナス検知
- 「設定○です」断定 UI
- CDN・外部通信・店舗連携・クラウド同期
- スルーの日次マニュアル +1（セット単位が正）
- 示唆早見の大幅改修・他機種への示唆ログ展開
- 「だから通常B」等のアプリ断定

## ファイル構成

```
smaslo-ceiling-memo/
  index.html
  README.md
  VERSION
  _selfcheck/
    run.js
```

## 制限

- 天井「+α」の詳細は機種仕様に依存し、表示は各 gCap 基準
- チバリヨ2／南国の設定表はプレースホルダ
- 解析値の正しさは保証しない。判断は人が行う
- セット／示唆ログは南国のみ。全機種JSON書出にはセット履歴を含めない（v2キー別）
- 日付跨ぎの「今日」は端末ローカル日付依存
